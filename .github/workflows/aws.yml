name: Beelder-Deploy-Pipeline

permissions:
  id-token: write
  contents: read

on: 
  push:
    branches: [master]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"